# 小技巧

```java
   //可以使用这样的构造方法来提升对字符串的读取速度
   BufferedReader reader = new BufferedReader(
                    new InputStreamReader(
                            new FileInputStream("")
                    )
            );
```



